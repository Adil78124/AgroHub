name: Docker Build and Deploy

# Отключен - используем github-pages-deploy.yml
# on:
#   push:
#     branches: [ master, main ]
#     tags: [ 'v*' ]
#   pull_request:
#     branches: [ master, main ]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
#       
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
#       
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       if: ${{ secrets.DOCKER_USERNAME != '' && secrets.DOCKER_PASSWORD != '' }}
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}
#         
#     - name: Extract metadata
#       id: meta
#       uses: docker/metadata-action@v5
#       with:
#         images: adil78124/agrohub
#         tags: |
#           type=ref,event=branch
#           type=ref,event=pr
#           type=semver,pattern={{version}}
#           type=semver,pattern={{major}}.{{minor}}
#           type=raw,value=latest,enable={{is_default_branch}}
#           
#     - name: Build and push Docker image
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         platforms: linux/amd64,linux/arm64
#         push: true
#         tags: ${{ steps.meta.outputs.tags }}
#         labels: ${{ steps.meta.outputs.labels }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max
# 
#   deploy-to-production:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
#     
#     steps:
#     - name: Deploy to production server
#       uses: appleboy/ssh-action@v1.0.0
#       if: ${{ secrets.PRODUCTION_HOST != '' && secrets.PRODUCTION_USER != '' && secrets.PRODUCTION_SSH_KEY != '' }}
#       with:
#         host: ${{ secrets.PRODUCTION_HOST }}
#         username: ${{ secrets.PRODUCTION_USER }}
#         key: ${{ secrets.PRODUCTION_SSH_KEY }}
#         script: |
#           docker pull adil78124/agrohub:latest
#           docker stop agrohub || true
#           docker rm agrohub || true
#           docker run -d \
#             --name agrohub \
#             --restart unless-stopped \
#             -p 3000:3000 \
#             -e NODE_ENV=production \
#             adil78124/agrohub:latest
